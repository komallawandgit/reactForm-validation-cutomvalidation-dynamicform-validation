import { OrderBySelectedPipePipe } from './order-by-selected-pipe.pipe';

describe('OrderBySelectedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBySelectedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
